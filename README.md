*This project has been created as part of the 42 curriculum by alikhan, kmurray.*

# Description

# Instructions
compile the program with `make`, the run the program (push_swap), it takes a
list of unique signed integers. optionally one of [--simple, --medium,
     --complex, --adaptive] can be used before the numbers, to specify which
algorithm is used. the output is a list of instructions separated by newlines

[describe instructions]

## Checker
the checker checks if the output of the push_swap program is correct, it can be
compiled with `make bonus` and takes a list of unique signed numbers. the
instructions are passed on stdin separated by newlines

# Resources

# Algorithms


(feel free to rewrite all of this if you dont like it)
